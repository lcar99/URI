#include <stdio.h>
 
int main() {
 
	int NUMBER;
	scanf("%d", &NUMBER);
	int NH;
	scanf("%d", &NH);
	double SH;
	scanf("%lf", &SH);
	double SALARY = NH*SH;
	printf("NUMBER = %d\n", NUMBER);
	printf("SALARY = U$ %.2lf\n", SALARY); 
 	
 
    return 0;
}
